services:
  calculator:
    build:
      context: .
    image: secure-calculator:1.0.0
    read_only: true
    network_mode: none
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    user: "10001:10001"
    pids_limit: 32
    mem_limit: 128m
    cpus: 0.50

